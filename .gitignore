# Neovim plugin lock (regenerated by lazy.nvim)
lazy-lock.json
/nvim.log

# Zsh private/machine-specific config
zsh-private.sh

# agent
.omx

# Project-local intermediate/eval artifacts (per CLAUDE.md convention)
tmp/

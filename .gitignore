# Neovim plugin lock (regenerated by lazy.nvim)
lazy-lock.json

# Zsh private/machine-specific config
zsh-private.sh

# Hyprland runtime files
.initial_startup_done
.current_wallpaper
monitors.conf
workspaces.conf

# Nix artifacts (if leftover)
result

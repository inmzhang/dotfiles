# Memory Summary

- Clarify ambiguous requirements early.
- Prefer sandbox-first execution; escalate only on concrete failures.
- Use project-local `tmp/` for intermediate artifacts.
- Keep commit messages explanatory for non-obvious tradeoffs.
- Use `update_plan` for multi-step tasks and keep status current.
- Record incidental follow-up issues in `SESSION.md`.

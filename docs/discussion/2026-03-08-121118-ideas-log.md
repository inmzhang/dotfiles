# Ideas Session — 2026-03-08 12:11

## Phase 0

### Checkpoint 1
- User invoked the `ideas` skill.
- Assistant checked for prior context in `docs/discussion/user-profile.md`, prior `*-ideas-log.md` files, and survey registries in `~/.claude/survey/` and `.claude/survey/`.
- Result: no prior profile, no prior idea logs, and no survey registry files were found.
- Assistant created this session log and prepared to begin Phase 0.
### Checkpoint 2 — Phase 0 complete / entering Phase 1
Assistant asked the user for their research background, including field, stage, tools, prior work, and what kinds of problems interest them.

User response (substantive content):
- They are a PhD student studying quantum computing and quantum error correction.
- Their focus is realistic hardware realization on nearest-neighbor connectivity superconducting qubit systems.
- Recently they have become interested in surface-code compilation at both the logical and physical levels.
- Logical level goal: compile a quantum algorithm/circuit into a surface-code spacetime / pipe diagram composed of primitive building blocks.
- Physical level goal: compile a surface-code spacetime / pipe diagram into instruction-level physical gate circuits that can run on simulators or real quantum hardware.
- For logical compilation, they want to develop a new algorithm that optimizes and compiles quantum algorithms through ZX-diagrams and then into pipe diagrams.
- Their optimization goals are spacetime volume reduction and/or space-constrained compilation that fits an algorithm into limited area while minimizing total duration.
- For physical compilation, they are developing the `bloc` project at `/home/inm/workspace/bloc`.
- One open question in that work is the most suitable compilation target for circuits with non-Clifford gates, where the computation is intrinsically nondeterministic and classical-quantum hybrid.
- Current candidate targets they named: Q#, OpenQASM 3, and tket2.

Assistant identified this as a strong Phase 1 entry point because the user already has two concrete directions and a live systems project.
### Checkpoint 3 — urgency and narrowing
Assistant summarized the user's profile and highlighted a key connection: the physical-level question is really about the right semantic object for non-Clifford fault-tolerant compilation, not merely a gate set.

Assistant restated understanding:
- The user cares about both logical compilation (`algorithm -> ZX -> pipe / spacetime diagram`) and physical compilation (`pipe / spacetime diagram -> executable control-level program`).
- The user has strengths in QEC and realistic superconducting constraints.
- The physical-level question likely needs to connect to the `bloc` project.

Assistant asked which direction is more urgent and whether there are timeline/advisor constraints.

User response:
- The physical/compiler-target question is more urgent.
- The timeline is urgent for this week or so.

Assistant decided to prioritize a rapid, evidence-based comparison of candidate compilation targets for the physical-level compilation problem, informed by the local `bloc` project and current official documentation/specifications.
### Checkpoint 4 — Phase 1 search and comparison for physical/compiler-target question
Conversation since last save:
- User clarified that the urgent direction is the physical/compiler-target question.
- User added that the timeline is urgent, on the scale of this week or so.
- Assistant decided to prioritize a rapid evidence-based comparison of candidate compilation targets, informed by the local `bloc` project and official documentation/specifications.

Local repository context gathered from `/home/inm/workspace/bloc`:
- `AGENTS.md` describes the pipeline as `.blog DSL -> block graph -> physical circuit -> Stim / OpenQASM3`.
- `docs/bloc_compile_design.md` says the compiler redesign preserves a statement-tree IR with `Loop`, `Branch`, `Action`, `DeclareDetector`, and `DeclareObservable` in `GlobalStatement`.
- The design doc explicitly says recursive control flow is essential for OpenQASM3 emission.
- The design doc also states: Stim backend rejects `Branch` because it is Clifford-only, while the OpenQASM3 backend emits `if/else` control flow with the classical condition variable.
- `crates/bloc_compile/src/ir/compiled.rs` defines `CompiledProgram` with `statements`, `num_registers`, and `GlobalStatement::{Moment, Tick, Loop, Branch, Action, DeclareDetector, DeclareObservable}` plus `GlobalTarget::Register` and `RegisterTarget::{Absolute, Relative}`.
- `docs/prior_art_tqec_and_gen.md` notes bloc is a strict superset of tqec because it adds `T`, `Y`, `SelectiveMeasurement`, and time-indexed `Action`s for branching and feedback. The comparison table there already anticipates OpenQASM3 as the main output for bloc, with Stim possibly later.

External primary-source checks performed:
1. **OpenQASM 3 official language sources** (`openqasm/openqasm` repository):
   - `source/language/classical.rst` contains first-class `bool` logic, `if`, `for`, `while`, `break`, `continue`, and `switch`.
   - The examples in the spec include measurement results used in conditions (`bit output = measure qubit; if (...) { ... }`).
   - `source/language/delays.rst` contains `duration`, `stretch`, `delay`, `durationof`, and `box`, i.e. explicit timing / alignment constructs.
   - This suggests OpenQASM3 is directly capable of expressing measurement-driven control and hardware-oriented timing structure.

2. **HUGR / tket2-related official sources** (`Quantinuum/hugr`):
   - `hugr-core/src/ops/controlflow.rs` defines `TailLoop`, `Conditional`, `CFG`, and typed control-flow/dataflow structures.
   - This indicates strong expressive power for classical-quantum hybrid control, but at the level of a typed compiler IR / graph representation rather than a hardware-facing text format.
   - Assistant inference: HUGR looks semantically rich, but likely introduces more integration/lowering work for `bloc` in the short term.

3. **Q# official docs** (Microsoft Learn):
   - The target profile documentation lists a progression from `Base` to `Adaptive_RI`, where `Adaptive_RI` allows mid-circuit measurement and reset, and `Adaptive_RIF` further adds branching based on measurement results.
   - Assistant inference: Q# certainly understands adaptive quantum programs, but through the lens of a full language plus target-profile restrictions rather than a low-level backend interchange format.

4. **QIR-related official sources**:
   - QIR Alliance scope document describes QIR as an LLVM-based many-to-many intermediate representation intended to support varying hardware capabilities and progressive classical-quantum interaction.
   - Quantinuum system documentation reports a transition from OpenQASM to QIR as the default language for submitting circuits to their API, with OpenQASM as an older option and QIR becoming the preferred one.
   - Assistant inference: this is a strong sign that the ecosystem may be drifting toward QIR for backend interchange, especially where adaptive/hybrid execution matters.

Reasoning developed during comparison:
- The user's real need is not merely "pick a gate format" but "choose the right semantic object for adaptive fault-tolerant protocols with classical feedback and nondeterministic non-Clifford structure."
- For this use case, there is an important distinction between:
  1. an **internal compiler IR** that can faithfully represent branching, loops, scheduling, waits, detector/observable metadata, and resource-state semantics; and
  2. an **external emission / interchange target** that tools or hardware ecosystems can consume.
- `bloc` already has an internal statement-tree IR whose structure is unusually well matched to this problem.
- OpenQASM3 appears to be the best fit among the user's three named candidates for immediate external emission because it already natively matches measurement-driven `if/else`, loops, classical variables, and explicit timing.
- Q# appears less suitable as the primary compilation target because it behaves more like a full source language with capability profiles than a backend interchange IR. Lowering `bloc` into Q# would likely feel semantically backwards and would expose the project to Q#'s language-level abstractions and profile constraints.
- HUGR / tket2 appears more suitable as a future research/optimization IR than as the first practical backend target this week. It is expressive, but it would likely require substantial translation effort and still need a later lowering path to executable hardware/simulator representations.
- A related but important twist is that QIR may be a better long-term external or secondary IR than Q# itself, because it offers general hybrid classical-quantum semantics and has visible ecosystem pull from providers like Quantinuum.

Draft recommendation prepared for the user:
- **Near-term:** keep `bloc`'s own statement-tree IR as the authoritative internal representation; emit **OpenQASM3** first.
- **Do not** make Q# the primary target IR.
- **Treat tket2/HUGR as optional future work** if the goal becomes typed optimization and interoperability rather than immediate executable emission.
- **Keep an eye on QIR** as a secondary research direction or later backend, especially if the user wants a more future-proof adaptive IR story beyond this week's deadline.
### Checkpoint 5 — entering Phase 2 on concrete target contract
Assistant gave a concrete recommendation:
- Keep `bloc`'s own statement-tree IR as the semantic source of truth.
- Use OpenQASM 3 as the first external target.
- Avoid making Q# the primary compilation target.
- Treat tket2/HUGR as a future optimization / research IR rather than the first backend target.
- Keep QIR in mind as a possible later backend or research direction.

Assistant supported this with local `bloc` references and official sources for OpenQASM3, HUGR/tket2, Q#, and QIR.

User response:
- "yes"

Assistant interpretation:
- The user wants to continue into a concrete design for `bloc`'s target contract: exact OpenQASM3 subset, mapping from `CompiledProgram` to emitted constructs, handling of `DecoderWait`, and representation of detector/observable metadata.
### Checkpoint 6 — narrowing the decoder result representation
Conversation since last save:
- Assistant proposed a concrete target contract for `bloc`: keep `CompiledProgram` as the semantic source of truth and use OpenQASM 3 as the first external target.
- Assistant proposed a disciplined OpenQASM 3 subset: declarations, standard gates, `measure`, `reset`, `if/else`, fixed-count `for`, concrete `duration`/`delay`, and an opaque decoder `extern` hook.
- Assistant argued that `DecoderWait` should not be represented merely as `delay`; instead, maintenance / syndrome-extraction rounds should be emitted explicitly while waiting for decoder output, with `delay` reserved for genuine physical idle.
- Assistant proposed that detector/observable annotations be emitted as `bloc`-namespaced pragmas because OpenQASM 3 has no native detector/observable semantics.
- Assistant identified `Branch.condition: String` in `CompiledProgram` as a weak spot and suggested replacing it with a typed classical condition / expression IR.
- Assistant then asked a key design-fork question: should the decoder result be modeled as a compact correction opcode such as `uint`, or as structured flags / `bit[k]` values?
- User replied by asking what the main difference is between those two choices.

Assistant's intended clarification:
- The central distinction is whether the decoder returns an **action** (`uint`/opcode, closed-world semantics) or returns **facts** (`bit[k]`/flags, compositional semantics).
- This choice affects coupling between decoder and compiler, ease of branch emission, compositionality of classical logic, debugability, and how easily the representation scales when more adaptive protocol decisions are added beyond simple Pauli frame updates.
### Checkpoint 7 — backend preference under a two-month timeline
Conversation since last save:
- User asked: what is the main difference between representing decoder results as a compact `uint`/opcode versus structured `bit[k]` flags?
- Assistant answered that the core difference is whether the decoder returns an **action** (`uint`) or **facts** (`bit[k]`).
- Assistant explained tradeoffs:
  - `uint` gives a tiny runtime interface and easier branch emission, but increases coupling because the opcode meanings are convention-specific and harder to extend.
  - `bit[k]` keeps semantics explicit, composable, and easier to debug/verify, but adds more classical logic and requires invariants to avoid illegal combinations.
- Assistant suggested a likely hybrid design for `bloc`: packed field for Pauli-frame update plus separate flags for higher-level protocol decisions.
- User then asked a new planning question: if the time limit is extended to two months, which backend target would the assistant prefer?

Assistant's current reasoning direction:
- The crucial distinction is now between **first practical external target** and **more ambitious architectural target**.
- With more time, alternative targets like HUGR/tket2 or QIR become more viable because richer typed/control-flow semantics may justify the extra integration cost.
- However, because the user's project is about realistic hardware realization on nearest-neighbor superconducting systems, explicit timing/control and executability still strongly favor OpenQASM3 as the first practical backend.
- A likely refined answer is: with two months, keep OpenQASM3 as the first backend target if forced to pick one executable target, but spend the extra time making the internal `bloc` IR typed enough that a second backend (likely QIR or HUGR-derived) becomes straightforward later.
### Checkpoint 8 — user asks for a deeper explanation of typed adaptive IR
Conversation since last save:
- User asked: if the task horizon is two months, which backend target would the assistant prefer?
- Assistant answered that even on a two-month horizon, OpenQASM 3 remains the preferred **first** backend target because it best matches adaptive fault-tolerant control with explicit timing on realistic hardware.
- Assistant clarified that the strategic shift with two months is not the first backend target, but investing more in `bloc`'s internal adaptive IR so that multiple backends become possible later.
- Assistant ranked likely backend priorities as: OpenQASM3 first, QIR second, HUGR/tket2 as a researchy semantic target, and Q# last for this use case.
- User then asked for a deeper explanation of the typed adaptive IR itself: its purpose, design principles, and how it integrates with different backends.

Assistant plan:
- Ground the answer in the existing `bloc` design docs and measurement typestate definitions.
- Explain typed adaptive IR as the internal semantic layer that represents adaptive fault-tolerant protocols explicitly while staying backend-agnostic.
